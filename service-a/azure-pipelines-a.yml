parameters:  
  name: Default



stages:
- stage: Build  
  jobs:
  - job: TEST_JOB_${{ parameters.name }}
 
    steps:
    - script: |
        echo "Hello from A"
    displayName: 'set node version to'
